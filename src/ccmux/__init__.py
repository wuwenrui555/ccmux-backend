"""ccmux — Claude-tmux backend library.

**Import from `ccmux.api` for the public surface.** This package root is
deliberately empty so `from ccmux import X` fails loudly rather than
silently routing through a re-export.
"""
